# =====================================================
# HyCON Equipment Management System - Configuration
# =====================================================
# Copy this file to .env and update with your actual values

# =====================================================
# DATABASE CONFIGURATION (REQUIRED)
# =====================================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://hycon_user:your_password_here@localhost:5432/hycon_db

# =====================================================
# SECURITY (REQUIRED - CHANGE IN PRODUCTION!)
# =====================================================
# Generate a secure random key using:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-this-to-random-32-character-string

# JWT settings
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =====================================================
# APPLICATION SETTINGS
# =====================================================
# Environment: development, staging, or production
ENVIRONMENT=production

# Enable/disable debug mode (set to False in production)
DEBUG=False

# Frontend URL (for CORS configuration)
# Examples:
#   - http://localhost:5173 (local development)
#   - http://your-company-domain.com (production)
#   - http://10.0.0.100 (internal IP)
FRONTEND_URL=http://your-company-domain.com

# =====================================================
# ACTIVE DIRECTORY / LDAP CONFIGURATION
# =====================================================
# Enable/disable LDAP authentication (set to false to disable AD login)
LDAP_ENABLED=true

# Active Directory server details
LDAP_SERVER=WIN-PALNGRANPOS.hyconlab.com
LDAP_PORT=389
LDAP_USE_SSL=false

# Domain configuration
LDAP_DOMAIN=hyconlab.com
LDAP_NETBIOS=HYCONLAB

# Search base (where to look for users)
LDAP_BASE_DN=DC=hyconlab,DC=com
LDAP_USER_SEARCH_BASE=CN=Users,DC=hyconlab,DC=com

# NOTE: Users will authenticate with their domain credentials
# Accepted formats: username, HYCONLAB\username, or username@hyconlab.com
# User roles are automatically assigned based on AD group membership

# =====================================================
# EMAIL CONFIGURATION (OPTIONAL)
# =====================================================
# Required for sample submission notifications
# Leave empty to disable email features

# SMTP server settings
SMTP_HOST=smtp.company-email.com
SMTP_PORT=587

# SMTP credentials
SMTP_USER=hycon@company.com
SMTP_PASSWORD=email_password_here

# Email sender information
SMTP_FROM_EMAIL=hycon@company.com
SMTP_FROM_NAME=HYCON Lab Management

# =====================================================
# NOTES FOR IT DEPARTMENT
# =====================================================
# 1. This file contains sensitive information - never commit to git
# 2. Generate a strong SECRET_KEY for production
# 3. Use environment-specific DATABASE_URL
# 4. Set FRONTEND_URL to actual production domain
# 5. Configure email settings if notifications are needed
